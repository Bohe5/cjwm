<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">
    <!--
     insert标签：单条插入订单主数据
     id="insert"：与OrderMapper接口中insert方法名一致，唯一标识该SQL
     parameterType="Orders"：指定传入参数类型为Orders实体类
     useGeneratedKeys="true"：开启主键自动生成（适配MySQL自增主键）
     keyProperty="id"：将生成的主键ID回写到Orders对象的id属性中（便于后续关联订单详情）
   -->
    <insert id="insert" parameterType="Orders" useGeneratedKeys="true" keyProperty="id">
        insert into orders (number,status,user_id,address_book_id,order_time,checkout_time,pay_method,pay_status,
                            amount,remark,phone,address,consignee,estimated_delivery_time,delivery_status,
                            pack_amount,tableware_number,tableware_status)
        values
            (#{number},#{status},#{userId},#{addressBookId},#{orderTime},#{checkoutTime},#{payMethod},#{payStatus},
             #{amount},#{remark},#{phone},#{address},#{consignee},#{estimatedDeliveryTime},#{deliveryStatus},
             #{packAmount},#{tablewareNumber},#{tablewareStatus})
    </insert>
    <!--
      update标签：动态更新订单信息（聚焦订单状态、支付状态等关键变更）
      id="update"：与OrderMapper接口中update方法名一致
      parameterType="com.sky.entity.Orders"：指定传入参数为Orders实体类
    -->
    <update id="update" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' ">
                cancel_reason=#{cancelReason},
            </if>
            <if test="rejectionReason != null and rejectionReason!='' ">
                rejection_reason=#{rejectionReason},
            </if>
            <if test="cancelTime != null">
                cancel_time=#{cancelTime},
            </if>
            <if test="payStatus != null">
                pay_status=#{payStatus},
            </if>
            <if test="payMethod != null">
                pay_method=#{payMethod},
            </if>
            <if test="checkoutTime != null">
                checkout_time=#{checkoutTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deliveryTime != null">
                delivery_time = #{deliveryTime}
            </if>
        </set>
        where id = #{id}
    </update>
    <!--
      select标签：订单分页查询（支持多条件筛选）
      id="pageQuery"：与OrderMapper接口中pageQuery方法名一致
      resultType="Orders"：查询结果映射到Orders实体类
    -->
    <select id="pageQuery" resultType="Orders">
        select * from orders
        <where>
            <if test="number != null and number!=''">
                and number like concat('%',#{number},'%')
            </if>
            <if test="phone != null and phone!=''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
        </where>
        order by order_time desc
    </select>
    <!--
      select标签：按条件统计订单总金额
      id="sumByMap"：与OrderMapper接口中sumByMap方法名一致
      resultType="java.lang.Double"：返回结果为Double类型（总金额）
    -->
    <select id="sumByMap" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="begin != null">
                and order_time &gt; #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <!--
     select标签：按条件统计订单数量
     id="countByMap"：与OrderMapper接口中countByMap方法名一致
     resultType="java.lang.Integer"：返回结果为Integer类型（订单数量）
   -->
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="begin != null">
                and order_time &gt; #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <!--
     select标签：查询销量Top10商品
     id="getSalesTop10"：与OrderMapper接口中getSalesTop10方法名一致
     resultType="com.sky.dto.GoodsSalesDTO"：结果映射到商品销量DTO（含商品名、销量）
   -->
    <select id="getSalesTop10" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name,sum(od.number) number
            from order_detail od,orders o
            where od.order_id=o.id and o.status=5
        <if test="begin != null">
            and o.order_time &gt; #{begin}
        </if>
        <if test="end != null">
            and o.order_time &lt; #{end}
        </if>
        group by od.name <!-- 按商品名分组，统计单个商品的总销量 -->
        order by number desc <!-- 按销量降序排列 -->
        limit 0,10 <!-- 只取前10条，即销量Top10 -->
    </select>
</mapper>
